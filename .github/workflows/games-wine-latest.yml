name: Build games-wine-latest

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "games/wine-latest/*"
      - .github/workflows/games-wine-latest.yml
      - "games/spark-utils/spark-utils.sh"

jobs:
  build:
    uses: ./.github/workflows/docker-build.yml
    with:
      context: ./games
      file: ./games/wine-latest/Dockerfile
      tags: ghcr.io/sparkedhost/images:games-wine-latest
