name: Build PostgreSQL

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "databases/postgres/**/*"
      - .github/workflows/postgres.yml

jobs:
  build:
    name: "PostgreSQL ${{ matrix.version }}"
    strategy:
      fail-fast: false
      matrix:
        version:
          - 13
          - 14
          - 15
          - 16
          - 17
    uses: ./.github/workflows/docker-build.yml
    with:
      context: ./databases/postgres
      file: ./databases/postgres/Dockerfile
      tags: ghcr.io/sparkedhost/images:postgres-${{ matrix.version }}
      build_args: POSTGRES_MAJOR=${{ matrix.version }}
    secrets: inherit
